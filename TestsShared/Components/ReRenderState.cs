namespace RazorRendererTests.Components
{
    /// <summary>Shared state for the parent/child re-render cascade test (read by both, owned by neither).</summary>
    public static class ReRenderState
    {
        public static int Value;
    }
}
